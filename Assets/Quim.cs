using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Quim : MonoBehaviour
{
    public int a = 0;
    // Start is called before the first frame update
    void Awake()
    {
        Debug.Break();
    }

    // Update is called once per frame
    void Update()
    {
        a++;
    }
}
